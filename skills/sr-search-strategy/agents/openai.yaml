interface:
  display_name: "SR-检索式设计与试检索"
  short_description: "SR-检索式设计、登录态检索与 Zotero 批量导出"
  default_prompt: "Use $sr-search-strategy to create a reproducible search protocol, achievement card, and a validated Zotero-importable RIS file for the records actually collected; use an explicitly authorized BrowserSkill session when login is required."

policy:
  allow_implicit_invocation: false
