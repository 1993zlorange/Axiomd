interface:
  display_name: "SR-场景与需求定义"
  short_description: "SR-场景与需求定义：古法首轮、人机协作、证据验收"
  default_prompt: "Use $sr-scenario-needs to complete SR-场景与需求定义 with my human baseline, a reviewable artifact, explicit human checkpoints, and an evidence-based closure record."

policy:
  allow_implicit_invocation: false
