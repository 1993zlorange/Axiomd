interface:
  display_name: "SR-Baseline复现"
  short_description: "SR-Baseline复现：古法首轮、人机协作、证据验收"
  default_prompt: "Use $sr-baseline-reproduction to complete SR-Baseline复现 with my human baseline, a reviewable artifact, explicit human checkpoints, and an evidence-based closure record."

policy:
  allow_implicit_invocation: false
