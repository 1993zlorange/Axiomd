interface:
  display_name: "SR-结果完整性检查"
  short_description: "SR-结果完整性检查：古法首轮、人机协作、证据验收"
  default_prompt: "Use $sr-result-integrity to complete SR-结果完整性检查 with my human baseline, a reviewable artifact, explicit human checkpoints, and an evidence-based closure record."

policy:
  allow_implicit_invocation: false
