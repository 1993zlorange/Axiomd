interface:
  display_name: "SR-失效与回退设计"
  short_description: "SR-失效与回退设计：古法首轮、人机协作、证据验收"
  default_prompt: "Use $sr-failure-fallback to complete SR-失效与回退设计 with my human baseline, a reviewable artifact, explicit human checkpoints, and an evidence-based closure record."

policy:
  allow_implicit_invocation: false
