interface:
  display_name: "SR-内部审稿"
  short_description: "SR-内部审稿：古法首轮、人机协作、证据验收"
  default_prompt: "Use $sr-internal-review to complete SR-内部审稿 with my human baseline, a reviewable artifact, explicit human checkpoints, and an evidence-based closure record."

policy:
  allow_implicit_invocation: false
